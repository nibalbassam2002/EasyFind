{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "providers": ["php", "node"],
      "buildCmd": "composer install --no-scripts --ignore-platform-reqs && npm install && npm run build",
      "startCmd": "php artisan config:clear && php artisan migrate --force && php artisan serve --host 0.0.0.0 --port $PORT"
    }
  }
}